export class MergeSort<T> {

  // public sort(array: T[]) {

  // }

  // public comparitor = (a: number, b: number) =>
  //   a - b;

  // private mergeSort(array: T[], start = 0, end = array.length) {
  //   const middle = Math.floor((start - end) / 2);

  //   if (end - start <= 1) {
  //     return [];
  //   }

  //   this.mergeSort(array, start, middle);
  //   this.mergeSort(array, middle, end);

  //   return this.merge(array, start, middle, end)
  // }

  // private merge(array: T[], start: number,  middle: number, end: number) {

  // }
}
